# --- Environments (recreated by setup.sh) ---
.venv/
venv/

# --- Python bytecode / caches ---
__pycache__/
*.py[cod]
.pytest_cache/

# --- OS noise ---
.DS_Store

# --- Generated data & reports (regenerated by the pipeline; live-site
# snapshots are pinned by their own manifest hashes, not by git) ---
ms_data/
recommendation_run/
*.log

# --- Local-only Claude settings (skills stay tracked) ---
.claude/settings.local.json
